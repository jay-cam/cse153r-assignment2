## Project Structure

This project contains two separate task notebooks.

### Task 1: Symbolic Unconditioned Generation

- Notebook: `task1_symbolic_unconditioned.ipynb`
- GitHub-readable export: `task1_symbolic_unconditioned.py`
- Presentation section: 0:00–14:36 in the video

This notebook loads the POP909 dataset, extracts MELODY tracks, trains a Markov baseline and an LSTM next-token model, generates MIDI outputs, and evaluates the generated melodies.

### Task 2: <Task Name>

- Notebook: `task2_<name>.ipynb`
- GitHub-readable export: `task2_<name>.py`
- Presentation section: 14:36–25:00 in the video

<Brief description of their task.>

## Viewing Notes

GitHub may fail to preview `.ipynb` notebooks with outputs. If that happens, open the `.py` export on GitHub to review the code and markdown, or download/open the `.ipynb` file in VS Code, Jupyter, or Google Colab.

The `.ipynb` notebooks are the primary files because they preserve outputs, tables, plots, and generated results used in the video presentation. The `.py` files are included only as readable exports for GitHub preview.
